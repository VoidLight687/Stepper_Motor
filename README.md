# Stepper_Motor
A MATLAB file with different Arduino commands that can run a stepper motor in clockwise or counterclockwise directions. Can also run a stepper motor in one phase, two phase, or half step movements. This serves as a script that can be used to pull these commands and put them into another script. 
